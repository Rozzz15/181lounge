# Plan: Remove all branch/store content

## Overview
181 Lounge does not have branches. Remove all store locator, branch listing, and "100+ Stores" references from the website.

## Files to Delete
1. `src/components/stores/stores-client.tsx` — full stores page component
2. `src/components/home/store-locator-preview.tsx` — homepage store preview section
3. `src/pages/Stores.tsx` — stores page wrapper
4. `src/app/stores/page.tsx` — Next.js stores page

## Files to Edit

### Navigation & Routing
- `src/components/layout/header.tsx` (line 12) — remove `{ href: '/stores', label: 'Find a Store' }` from navLinks
- `src/components/layout/footer.tsx` (line 23) — remove `{ href: '/stores', label: 'Store Locator' }`
- `src/App.tsx` (lines 11, 32) — remove Stores import and `/stores` route

### Homepage
- `src/pages/Home.tsx` (lines 9, 24) — remove StoreLocatorPreview import and usage
- `src/app/page.tsx` (lines 8, 20) — remove StoreLocatorPreview import and usage

### Content Updates
- `src/components/home/about-section.tsx` (line 146) — change "100+ Stores" stat to something like "100+ Menu Items" or remove
- `src/components/home/delivery-section.tsx` (line 30) — remove `href: '/stores'` from delivery links
- `src/components/story/our-story-client.tsx` — update timeline entries and stats that mention "100+ stores"
- `src/components/contact/contact-client.tsx` (line 368) — update FAQ about store hours

### Admin Dashboard
- `src/components/admin/admin-dashboard.tsx` — remove stores tab, mockStores data, and store-related UI

## Verification
- `npm run build` passes
- No broken links to `/stores`
- Homepage no longer shows store locator section
- Nav and footer no longer link to stores
